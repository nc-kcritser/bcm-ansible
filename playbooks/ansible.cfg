[defaults]
inventory = ./inventory/hosts
retry_files_enabled = False

# Vault password comes from scripts/vault-pass-prompt.sh:
# uses $ANSIBLE_VAULT_PASSWORD if set, otherwise prompts on the terminal.
vault_password_file = ./scripts/vault-pass-prompt.sh